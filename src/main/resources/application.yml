server:
  port: 8081

spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/dev

keycloak:
  realm: dev
  auth-server-url: http://localhost:8080
  resource: budget_buddy_dev
  credentials:
    secret: hatinAD4ZtFQ0aI8rOXA9EiR2UziMfiq
  use-resource-role-mappings: true

